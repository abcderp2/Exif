# 変更時の確認

このサイトは、依存関係と処理範囲を小さく保つことで、低性能端末でも保守しやすい状態を維持します。

## 変更前

目的を一つに絞ります。機能追加と見た目の全面変更を同じ変更へ混ぜないでください。

外部ライブラリ、CDN、有料API、サーバー処理、アクセス解析、広告、Cookie、永続ストレージは既定で追加しません。追加が必要な場合は、端末内完結と追加課金を必要としない設計が崩れないことを先に説明してください。

対応形式を増やす場合は、形式判定、構造検査、再エンコード、処理後検証、低性能端末の上限、セキュリティ上の動作内容を一緒に実装します。accept属性だけを増やさないでください。

免責事項、対応形式、処理上限、外部通信の説明を変える場合は、サイト、README、SECURITY.mdの記載を同じPull Requestで揃えてください。

## 必須確認

Node.js 20以降で次を実行します。

```sh
npm run verify
```

簡易サーバーを起動します。

```sh
python3 -m http.server 8000
```

スマートフォン相当の狭い幅、タブレット相当の幅、パソコン相当の幅で確認します。

JPEG、透明PNG、WebPを一つずつ用意し、入力と同じ形式、JPEG、PNG、WebPへの変換を確認します。

ExifまたはPNGテキストを含むテスト画像で、処理前に分類が表示され、処理後に安全確認を通過することを確認します。

やり直す、個別削除、個別再処理、個別保存、完了分保存、JSON分析結果の保存を確認します。

ブラウザの開発者ツールで、画像処理による外部通信が発生していないことを確認します。

サイト上に無保証、自己の責任、原本保管、重要用途での別手段による確認が明記されていることを確認します。

## 自動ブラウザ監査

Pull Requestを作成すると.github/workflows/browser-audit.ymlが自動実行されます。

監査は公開サイトと変更中のローカルサイトをChromiumで実際に操作し、三形式の入力と出力、処理後検査、画像とJSONの保存、再処理、リセット、低性能端末向け縮小、スマートフォンとタブレットとパソコンの横はみ出し、外部通信、JavaScript例外を確認します。

手元で再実行する場合は、ローカルサーバーを起動したうえで次を実行します。

```sh
npm install --no-save --no-package-lock @playwright/test@1.55.0
npx playwright install chromium
node tests/browser-audit.mjs
```

Playwrightは監査時だけ使います。公開サイトの実行時依存関係へ追加しません。

## コード上の約束

利用者由来の文字列をinnerHTMLへ渡しません。

Object URLを作った場所には、再処理、削除、リセット、終了時の解放処理を用意します。

新しい上限値には理由をREADMEへ記載します。

解析の結果を断定的な鑑定表現へ変えません。検出できる領域と検出できない領域を分けます。

エラーは利用者が次に取れる操作を含む日本語にします。

## Pull Request

変更内容、変更理由、利用者への影響、確認したコマンド、自動監査の結果、手動確認したブラウザと端末幅、残る制限を記載してください。

mainへは原則としてsquash mergeし、問題があれば直前のマージコミットを一つの単位でrevertできる状態にします。

# 変更時の確認

このサイトは、依存関係と処理範囲を小さく保つことで、低性能端末と保守経験の少ない環境でも変更を追いやすい状態を維持します。

## 変更前

目的を一つに絞ります。機能追加と見た目の全面変更を同じ変更へ混ぜません。

外部ライブラリ、CDN、有料API、サーバー処理、アクセス解析、広告、Cookie、永続ストレージは既定で追加しません。追加が必要な場合は、端末内完結、追加課金なし、低性能端末対応が崩れないことを先に説明します。

対応形式を増やす場合は、形式判定、構造検査、再エンコード、処理後検証、低性能端末の上限、セキュリティ上の動作内容を一緒に実装します。accept属性だけを増やしません。

免責事項、対応形式、処理上限、外部通信、保存レポート、検索公開方針の説明を変える場合は、サイト、README、SECURITY.mdの記載を同じPull Requestで揃えます。

## ファイルの責務

metadata.jsは画像構造の解析だけを担当します。生のメタデータ値を画面やレポートへ返さない方針を維持します。

reports.jsは安全な分析オブジェクトからJSONとCSVを作ります。DOM、ネットワーク、画像デコードに依存させません。CSVへ列を追加する場合は、数式注入対策を通ることを確認します。

app.jsは画面状態、画像処理、保存ファイル名の選択を担当します。新しい処理は、リセット、個別削除、ページ終了時の解放経路も一緒に追加します。匿名化を追加する場合は、画像だけでなく分析レポートからも元の名前を除きます。

styles.cssは既存の配色と余白の印象を大きく変えず、280px以上の狭い画面から大画面まで横にはみ出さないことを優先します。accessibility.cssはフォーカス時のskip linkだけを担当し、通常表示のレイアウトを変えません。

robots.txtは公開クローラー方針だけを担当します。利用者の画像処理や外部通信を追加する場所として使いません。GitHub Pagesのプロジェクトサイトでは、ドメイン直下のrobots.txtとは配置が異なることをREADMEへ明記します。

## 必須確認

Node.js 20以降で次を実行します。

```sh
npm ci --ignore-scripts
npm run verify
```

簡易サーバーを起動します。

```sh
python3 -m http.server 8000
```

幅280px、320px、360px、768px、1024px、1440pxで横方向のはみ出しを確認します。スマートフォン横向きも確認します。

JPEG、透明PNG、WebPを用意し、入力と同じ形式、JPEG、PNG、WebPへの変換を確認します。ブラウザがWebP保存に対応しない場合は、選択肢が無効になることを確認します。

有効なJPEGへExifまたはGPS領域を付けたテスト画像で、処理前にExifと付加情報が検出され、処理後にExifとGPS領域がなくなり、保存後の再解析にも通過することを確認します。保存ファイル名は元の名前の維持と匿名連番をそれぞれ確認し、匿名化レポートに元の名前が残らないことも確認します。

複数画像の追加、一括処理、画面全体へのドラッグ、フォルダー拒否、重複拒否、やり直す、個別削除、個別再処理、個別保存、完了分保存、個別JSON、一括JSON、一括CSVを確認します。

CSVは、先頭がイコール、プラス、マイナス、アットマークのファイル名を表計算ソフトで開いても数式として実行されないことを確認します。

ブラウザの開発者ツールで、画像処理による外部通信が発生していないことを確認します。

サイト上に無保証、自己の責任、原本保管、重要用途での別手段による確認が明記されていることを確認します。免責事項の本文は残し、不要なページ内リンクを増やしません。

robots.txtに一般クローラー、OAI-SearchBot、GPTBot、Claude-User、Claude-SearchBot、ClaudeBot、Google-Extendedの許可設定があることを確認します。

## 自動ブラウザ監査

Pull Requestを作成すると.github/workflows/browser-audit.ymlが自動実行されます。

tests/accessibility-audit.mjsはフォーム部品のアクセシブルネーム、ファイル入力の説明関連付け、skip linkを確認します。tests/browser-audit.mjsはローカル版と公開版で基本機能を操作します。tests/release-audit.mjsはExifとGPS付きJPEGの除去、一括レポート、ドラッグ操作、順次保存、robots.txt、不要な免責リンクがないことを確認します。

mainへの反映時は、GitHub Pagesに今回のrobots.txtが配信されるまで待ってから、公開URLに対してrelease-auditを実行します。

手元で再実行する場合は、ローカルサーバーを起動したうえで次を実行します。

```sh
npm ci --ignore-scripts
npx playwright install chromium
AUDIT_LOCAL_URL=http://127.0.0.1:8000/ node tests/accessibility-audit.mjs
node tests/browser-audit.mjs
AUDIT_RELEASE_TARGET=http://127.0.0.1:8000/ node tests/release-audit.mjs
```

Playwrightはpackage-lock.jsonで固定した開発依存として監査時だけ使います。公開サイトの実行時依存関係へ追加しません。

## コード上の約束

利用者由来の文字列をinnerHTMLへ渡しません。

Object URLを作った場所には、再処理、削除、リセット、終了時の解放処理を用意します。

Canvasを作った場合は、処理後に小さくしてメモリ解放を促します。

非同期処理には中止経路を用意し、処理中のリセット後に古い結果を画面へ戻しません。

新しい上限値には理由をREADMEへ記載します。

解析の結果を断定的な鑑定表現へ変えません。検出できる領域と検出できない領域を分けます。

エラーは利用者が次に取れる操作を含む日本語にします。ブラウザ内部の例外文字列をそのまま表示しないようにします。

## Pull Request

変更内容、変更理由、利用者への影響、確認したコマンド、自動監査の結果、手動確認したブラウザと端末幅、残る制限を記載します。

mainへは原則としてsquash mergeし、問題があれば直前のマージコミットを一つの単位でrevertできる状態にします。

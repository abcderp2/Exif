{
  "name": "image-metadata-cleaner",
  "private": true,
  "version": "2.0.0",
  "description": "JPEG、PNG、WebPを端末内で相互変換し、付加情報を引き継がない静的ブラウザアプリ",
  "scripts": {
    "check": "node --check app.js && node --check metadata.js && node --check image-worker.js",
    "test": "node --test tests/*.test.cjs",
    "verify": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}

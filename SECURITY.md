# セキュリティ方針

## 対象

mainブランチと公開サイトの最新版を対象にします。過去のコミット、複製サイト、改変版、古いブラウザ固有の問題は、状況を確認したうえで対応範囲を判断します。

## 保護する対象

このサイトは、画像を外部へ送らずに付加情報を確認し、画素から新しい画像を作ることを目的としています。

入力画像、ファイル名、分析結果、作成した画像をサイト側で保存しません。ブラウザを閉じるか、やり直す操作をすると、ページが保持していた参照を解放します。

JSONとCSVには、検出した領域の分類だけを記録します。GPS座標、コメント本文、作者名などの生の値は出力しません。CSVは表計算ソフトの数式注入を避けるため、危険な先頭文字を無害化します。

## 信頼境界

処理は利用者のブラウザと端末内で完結します。GitHub Pagesは静的ファイルを配信しますが、画像の処理先にはなりません。

ブラウザの画像デコーダー、Canvas、Worker、端末の保存機能、GitHub Pagesの配信基盤は、このリポジトリのコードだけでは保証できません。ブラウザとOSは更新された状態で利用してください。

robots.txtと検索向けmeta要素は、公開ページをクローラーが訪問する際の方針です。認証、アクセス制御、秘密情報の保護にはなりません。利用者が選んだ画像をクローラーへ渡す処理はありません。

GitHub Pagesのプロジェクトサイトでは、このリポジトリのrobots.txtは /Exif/robots.txt として配信されます。ドメイン全体へ適用される標準上の配置先はドメイン直下の /robots.txt です。この違いをセキュリティ対策やアクセス制御として扱いません。

## 実装上の対策

対応入力をJPEG、PNG、WebPへ限定しています。

ファイル本体のシグネチャを確認し、拡張子や申告MIMEとの不一致を表示します。

ファイル個数、単体容量、端末別の合計容量、展開後の画素数、一辺の長さ、完了画像の保持量、解析するチャンク数、読み込むメタデータ領域に上限を置きます。

一件ずつ処理し、可能な環境ではOffscreenCanvasとWorkerを使います。Worker処理には45秒の上限を設けます。

Content Security Policyで外部通信、外部スクリプト、オブジェクト、フレーム、フォーム送信、メディア読込を禁止します。

表示内容はinnerHTMLを使わず、textContentとDOM APIで作ります。

ドラッグされたファイルを画面全体で受け止め、ブラウザがローカルファイルへ移動する操作を抑止します。画像以外とフォルダーは処理しません。

処理後のファイルを再解析し、個人情報領域または構造異常が検出された場合は保存用ファイルを破棄します。

## 分析の限界

検査は一般的なJPEG、PNG、WebP構造と主要なメタデータ領域を対象にします。未知の独自領域、破損ファイル、意図的に細工されたファイル、ブラウザ固有の再エンコード情報を完全に証明するものではありません。

色表示に必要な標準情報が出力へ生成される場合があります。これはGPSや撮影日時とは異なります。

重要な証拠、医療、法務、報道、研究用途では、このサイトだけを最終検証手段にしないでください。

## 無保証と利用者の責任

このサイトとソフトウェアは現状のまま提供されます。運営者は、動作、正確性、安全性、完全性、継続性、特定目的への適合性、権利非侵害、付加情報の完全な除去について、明示または黙示を問わず一切保証しません。

利用者は原本を別に保管し、処理後の画像と付加情報を別の手段でも確認してください。このサイトの利用と出力ファイルの使用は、利用者自身の判断と自己の責任で行ってください。

## ブラウザ監査

Pull Request、mainへの反映、手動実行時に、Chromiumを起動する監査を実行します。構文検査、単体テスト、三形式の入力と出力、ExifとGPS付きJPEGの検出と除去、保存後の再解析、一括レポート、ドラッグ追加、再処理、リセット、低性能端末向け縮小、画面幅、外部通信、JavaScript例外を確認します。

mainへの反映時は、GitHub Pagesへ今回のファイルが配信されたことを確認してから、公開URLでもExif除去と主要機能を操作します。

この監査は確認範囲を広げるための回帰検査であり、安全性や完全性を保証するものではありません。ブラウザ、OS、画像デコーダー、未知のファイル構造をすべて網羅するものでもありません。

## 脆弱性の報告

公開Issueへ、個人情報を含む画像、GPS座標、非公開URL、認証情報、攻撃に利用できる詳細を添付しないでください。

リポジトリのSecurityタブに非公開の脆弱性報告機能が表示される場合は、その経路を使用してください。利用できない場合は、機密情報を含めずにIssueを作成し、非公開で共有する必要があることだけを記載してください。

再現に画像が必要な場合は、個人情報を含まない最小のテスト画像を新しく作り、ブラウザ名と版、OS名、入力形式、画像のおおよその大きさ、選択した出力形式、表示されたエラー、再現手順を記載してください。

緊急性が低く、機密情報を含まない不具合はGitHub Issuesで報告できます。
